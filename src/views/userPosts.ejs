<!-- views/userPosts.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Posts</title>
</head>
<body>

  <h1>User Posts</h1>

  <% if (posts.length > 0) { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <h2><%= post.nameUser %></h2>
          <p><%= post.publicationDescription %></p>
          <img src="<%= post.image %>" alt="<%= post.nameUser %>">
          
          <!-- Incluye el formulario de campos -->
          <% include('_form_fields.ejs', { post: post }) %>
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No hay posts disponibles para este usuario.</p>
  <% } %>
  
  <a href="/">Volver a la página principal</a>

</body>
</html>
